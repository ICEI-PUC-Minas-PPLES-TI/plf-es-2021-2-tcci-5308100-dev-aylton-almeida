import 'package:flutter/material.dart';

class OrderListTile extends StatelessWidget {
  const OrderListTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
